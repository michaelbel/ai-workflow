---
name: multiexpert-review
argument-hint: "[--profile=<implementation-plan|test-plan|spec>] [--experts=<expert,…>] [--max-cycles=<N>] [--reviewer-model=<tier>] [--quick] <артефакт или путь>"
description: >
  Ревью плана, спецификации или тест-плана панелью независимых экспертных агентов по протоколу
  PoLL (Panel of LLM Evaluators) до того, как решение зафиксировано. Triggers: "review the plan",
  "review the spec", "panel review", "validate the approach", "what did I miss?", «отревьюй план»,
  «что я упустил». НЕ для ревью кода — это агент code-reviewer.
---

# Multi-Expert Review

Движок независимого многоагентного ревью документального артефакта (план, спека, тест-план) с
последующим синтезом консенсуса. Семантика конкретного вида артефакта живёт в **профилях**,
`profiles/<имя>.md`. Сам движок к виду артефакта безразличен: он обнаруживает и маршрутизирует, но
никогда не зашивает рубрику одного вида артефакта в собственное тело.

Протокол — PoLL: независимое параллельное ревью каждым агентом, структурированный вывод с severity и
confidence, синтез со взвешиванием по confidence, расхождения выносятся как «требует решения», а не
разрешаются молча.

Каждый ревьюирующий агент обязан сверить артефакт с разделами `## Non-negotiables` в применимых
файлах `CLAUDE.md` (корень проекта, глобальный, плагинный) до того, как сформирует мнение. Любой
предложенный подход, нарушающий non-negotiable, автоматически блокер: severity critical, confidence
100, вне фильтра отчётности и вне обсуждения компромиссов.

## Инварианты движка (профили их не переопределяют)

Профили объявлять это НЕ МОГУТ — это константы движка:

- **Структура вывода ревью** — Summary / Domain Relevance / Issues с severity, confidence, issue и
  suggestion (зафиксирована в промпте шага 3).
- **Правила агрегации** — схождение эскалирует, противоречия выносятся, взвешивание по confidence
  (зафиксированы в шаге 4).
- **Переходы автомата состояний** — зафиксированы в этом файле.
- **Предел цикла правок** — 2 цикла за один вызов по умолчанию. Вызывающий может явно раздвинуть его
  флагом `--max-cycles=N` **только** при возобновлении после того, как пользователь разрешил
  эскалированный блокер — см. «После эскалации» ниже. Молчаливого или автоматического снятия предела
  нет: без явного `--max-cycles` и без предшествующей пользовательской эскалации предел остаётся 2.

  Предел равен двум, а не трём, по замеру: панель находит трудную область за один-два цикла, дальше
  перестаёт платить. На трёх прогонах одной фичи (спека v1 — 3 цикла, план — 9, спека Phase 1.5 — 4)
  каждый цикл с третьего не закрывал область, а переносил дефект внутри неё; разрешало эскалацию
  сужение объёма пользователем, и оба раза оно срабатывало немедленно. Девятицикловый прогон, где
  вместо сужения шли новые раунды, не сошёлся вообще.
- **Гейт готовности** — артефакт нельзя пометить `approved`/пропустить дальше по конвейеру без
  фактического `PASS` или `CONDITIONAL` от этого движка на его текущем содержимом. Правка, внесённая
  по указанию пользователя после эскалации, не заменяет повторный прогон — она открывает право на
  него. Единственное исключение — явный оверрайд пользователя своими словами («не гоняй ревью ещё
  раз», «пропусти проверку»), который вызывающий обязан записать отдельным, отличимым от `approved`
  значением (см. «После эскалации»), а не выдавать за пройденное ревью.
- **Скелет промпта ревью** — профили добавляют через `## Prompt augmentation` и никогда не заменяют.
- **Модель ревьюеров** — по умолчанию `model: opus`, effort высокий, для каждого агента, запускаемого
  движком на шаге 3, независимо от модели вызывающей сессии: соответствие уже действующему правилу
  routing из `~/.claude/rules/orchestration.md` («security/perf/UX-ревью, отладка первопричины →
  opus на высоком effort»). Молчаливого наследования модели вызывающей сессии нет — это и есть баг,
  который правило устраняет. Override — флагом `--reviewer-model=<tier>` на конкретный вызов (понизить
  для дешёвого/тривиального артефакта, например `--quick` эквивалентен `--reviewer-model=sonnet`, либо
  повысить effort дальше). Без флага — всегда opus/high.

Негативный список запрещённых полей frontmatter и поведение ошибки `FORBIDDEN_PROFILE_FIELD` — в
`profiles/README.md`.

## Ход работы

Прочитать артефакт и определить профиль → обнаружить агентов, предварительно отобрать по
`profile.reviewer_roster` → запустить агентов параллельно на независимое ревью → собрать ревью →
синтезировать вердикт (агрегация движка плюс алфавит вердиктов из профиля) → показать вердикт и
обновить расписку, если она у профиля есть → PASS завершает; CONDITIONAL и WARN по политике профиля;
FAIL → починить артефакт в источнике → перепрогнать ревью теми же агентами с зафиксированным профилем.

**Запрещено:** пропускать «прочитать и определить» перед ревью, а также перезапускать определение
профиля на цикле 2 и дальше — профиль фиксируется на цикле 1.

**Предел циклов:** 2 всего за вызов (первый плюс один повторный), если вызывающий не передал
`--max-cycles`. Всё ещё FAIL после предела — эскалация пользователю типа `cycle_cap_exhausted`, **не**
тупик. Эскалация может сработать и раньше предела: два цикла подряд с `recurrence: true` (детектор
churn, см. Шаг 4) — эскалация типа `convergence_failure` немедленно, независимо от того, сколько
циклов осталось до `--max-cycles`. В обоих случаях — не автономная правка вне движка: пользователь
выбирает путь возобновления из меню (см. «После эскалации: возобновление» ниже), вызывающий действует
по выбору. Дальше по конвейеру идти можно только с фактическим PASS/CONDITIONAL этого движка либо с
явно записанным оверрайдом пользователя — не с фактом «пользователь сказал, как поправить».

## Сохранение состояния (устойчивость к сжатию)

Писать состояние в `./swarm-report/multiexpert-review-<slug>-state.md` (либо
`multiexpert-review-<YYYYMMDD-HHMM>-state.md`, если слаг неизвестен), по конвенциям шаблона
постоянного состояния из `~/.claude/references/context-resilience.md`.

**Источник слага** в порядке приоритета: явные аргументы вызывающего (`slug:`), frontmatter артефакта
`slug:`, имя файла артефакта без расширения, отметка времени как запасной вариант.

**Чтение унаследованного:** если файла со слагом нет, попробовать `./swarm-report/plan-review-state.md`
(остался с эпохи до переименования). Нашёлся — скопировать содержимое в новое имя со слагом и
продолжать на нём. Старый файл не удалять, это решает пользователь. Писать всегда в новое имя.

Структура файла состояния:

```markdown
# Multi-Expert Review State
Source: {plan_mode | file:<path> | conversation}
Profile: {implementation-plan | test-plan | spec | ...}   # фиксируется на цикле 1
Profile source: {caller_hint | frontmatter | path | signature | user_prompt}
Cycle: {N} of {M}   # M = 2 по умолчанию, расширяется --max-cycles при возобновлении после эскалации
Status: {detecting | reviewing | synthesizing | fixing | awaiting-user-resolution | done}

## Artifact Summary
{цель, технологии, объём — извлекается на шаге 1}

## Selected Agents
- {agent1} (рекомендован)
- {agent2} (рекомендован)

## Reviews Completed
- [x] {agent1} — {N critical, M major, K minor}
- [ ] {agent2} — ожидается

## Verdict History
### Cycle 1: {PASS | CONDITIONAL | FAIL | WARN}
- Blockers: {список}
- Improvements: {список}
- Recurrence: n/a (цикл 1)

### Cycle 2: {PASS | CONDITIONAL | FAIL | WARN}
- Blockers: {список}
- Improvements: {список}
- Recurrence: {true, тема и связь с находкой цикла 1 | false}

Churn streak: {N}   # подряд идущих циклов с recurrence: true; сбрасывается в 0 циклом без recurrence

## Escalations   # раздел появляется только если предел исчерпывался или сработал convergence_failure
### After cycle {M}: escalated {дата}, type: {cycle_cap_exhausted | convergence_failure}
- User resolution: {что решил пользователь — дизайн-решение, директива по фиксу, либо "overridden: <дословно>"}
- Resolution path: {extend_budget | raise_reviewer_model | hard_case_arbiter | back_to_previous_stage}
- Budget extended to: {новое M} | Overridden without re-review: {yes, с причиной}
```

Перечитывать файл перед каждым действием и пропускать уже завершённые шаги. Файл состояния **не
удалять** на эскалации, если ожидается возобновление — только когда движок достиг финального
PASS/CONDITIONAL/WARN либо пользователь явно оформил оверрайд (см. ниже).

## Шаг 1 — прочитать артефакт и определить профиль

Аргументы приходят одной свободной строкой:
`[--profile=<name>] [--experts=<список>] [--max-cycles=<N>] [--reviewer-model=<tier>] [--quick] <scope>`.
Все флаги необязательны — без `--profile` его определяет детектор, без `--experts` панель определяет
профиль, без `--max-cycles` предел — 2, без `--reviewer-model`/`--quick` модель ревьюеров — `opus` на
высоком effort (см. «Инварианты движка»). Неизвестный токен — остановиться и сказать, что именно не
понято; не угадывать.

`--reviewer-model=<tier>` переопределяет модель всех ревьюеров этого вызова явно (`sonnet`, `haiku`,
`opus` с другим effort). `--quick` — короткая форма для дешёвого/тривиального артефакта, эквивалент
`--reviewer-model=sonnet`; не путать с `--depth=quick` вызывающих скиллов (`write-plan`, `write-spec`)
— это разные флаги на разных уровнях, `--depth` управляет составом панели у вызывающего скилла,
`--quick` этого движка — только моделью.

`--max-cycles=<N>` задаёт потолок цикла правок для этого вызова (см. «Предел циклов» выше). Передавать
его можно в любой вызов, но по смыслу он существует для одного случая — возобновления после того, как
пользователь разрешил эскалированный блокер (см. «После эскалации: возобновление»); передача его на
первом вызове без предшествующей эскалации не запрещена технически, но не имеет обоснования в
протоколе и не должна происходить без явной причины, названной вызывающим.

`<scope>` — артефакт: искать в таком порядке: (1) активный вывод режима планирования в разговоре,
(2) ссылка на файл (пользователь указал `.md`), (3) инлайновое описание в разговоре, (4) спросить.
Зафиксировать источник — он нужен шагу 5.

**Определение профиля** — по цепочке приоритетов из `profiles/README.md`, §Detection precedence, это
канонический источник. Движок применяет описанную там семантику ошибок: `UNKNOWN_PROFILE_HINT` на
неизвестной подсказке вызывающего, и никогда молчаливый откат к профилю по умолчанию. Фиксация
профиля по циклу, валидация профиля по негативному списку и проверка соответствия инвентарю тоже
живут в `profiles/README.md` и применяются на каждом вызове до шага 2.

## Шаг 2 — обнаружить и отобрать агентов

### Обнаружение

Находить реальных агентов через `Glob("**/agents/*.md")` плюс встроенные субагенты из системного
промпта. Читать frontmatter каждого для подтверждения. Фантомных агентов не выдумывать.

**Разрешение коллизии коротких имён:** брать первое совпадение в порядке — (1) тот же плагин, что у
вызывающего, (2) соседний плагин семейства `developer-workflow-*`, (3) любой другой источник.
По-прежнему неоднозначно — падать громко:
`[multiexpert-review ERROR] AMBIGUOUS_REVIEWER: short-name <name> resolves to <paths>`. Это отдельная
ошибка от `NO_REVIEWERS_AVAILABLE`. Внутри семейства короткие имена уникальны, так что срабатывает
только на конфликтах вне его.

### Отбор по профилю

Использовать `profile.reviewer_roster`:

- **`primary`** — обязательный состав. Включать, если установлен; пропускать, если отсутствует.
- **`optional_if`** — по каждой записи включать, если regex из `when` совпал с содержимым артефакта И
  агент установлен.
- **Пустой primary и ни одного совпадения в optional** — откатиться на отбор по техническому
  соответствию (профиль implementation-plan на этом и стоит): просканировать артефакт на ключевые
  слова технологий, оценить агентов по совпадению технологий, по ценности для конкретной проблемы и
  по покрытию пробелов, отобрать двух-трёх и объявить состав строкой. Это отбор, а не рекомендация:
  подтверждения он не требует, рычаги те же, что ниже.

### Защита от единственного рецензента

Отобран ровно 1 агент:

- `profile.allow_single_reviewer: true` → продолжать. Вердикт несёт маркер
  `## Review Mode: single-perspective` (только в тексте вывода; схемы расписок объявляются профилем и
  поля `review_mode` не содержат).
- `profile.allow_single_reviewer: false` → падать громко:
  `[multiexpert-review ERROR] NO_REVIEWERS_AVAILABLE: profile <name> requires panel, only <agent> available`.

Ноль агентов → та же ошибка `NO_REVIEWERS_AVAILABLE` независимо от флага.

### `--experts` — состав панели

**Панель не подтверждается.** Без флага состав определяет профиль: движок объявляет его одной
строкой и запускает. Спрашивать «каких запускать» — значит предлагать
сократить сработавшую панель, а сокращать её нельзя: research, spec и test-plan — разные тексты с
разными failure modes, и «эту область уже смотрели в прошлом ревью» основанием не является.

| Значение | Действие |
|---|---|
| *(флаг не передан)* | Панель из `profile.reviewer_roster`, запуск без вопросов. |
| `<expert>[,<expert>…]` | Ровно названные эксперты вместо отбора по профилю. Профиль при этом не переопределяется — меняется только состав панели. |

Эксперты, названные в тексте запроса («отревьюй с `kotlin-engineer`»), эквивалентны
`--experts=kotlin-engineer`.

Несуществующее имя — падать громко:
`[multiexpert-review ERROR] UNKNOWN_EXPERT: <name> not found among discovered agents`.
Не подставлять похожего по имени и не пропускать молча.

Явный список, оставивший одного эксперта, подчиняется тем же правилам, что и пустой отбор:
`allow_single_reviewer` и `NO_REVIEWERS_AVAILABLE` из раздела выше.

## Шаг 3 — параллельное независимое ревью

Запустить каждого отобранного агента **одним сообщением**, параллельно, с `model: opus` и высоким
effort (либо переопределением `--reviewer-model`/`--quick` из шага 1) — см. «Инварианты движка».
`model: inherit` на этом шаге не использовать никогда: молчаливое наследование модели вызывающей
сессии — ровно тот баг, который правило устраняет.

### Промпт ревью (скелет движка)

```
You are reviewing a {artifact_type} as a {agent_role} expert.

## The Artifact
{full_artifact_text}

{PROFILE_PROMPT_AUGMENTATION}

## Your Task
Review this artifact from the perspective of your expertise. Be specific and actionable.

## Required Output Format

### Summary
2-3 sentence overall assessment from your perspective.

### Domain Relevance
One of: high | medium | low — how much this artifact touches your expertise.

### Issues
For each issue:

**Issue N: {short title}**
- **severity**: critical | major | minor
- **confidence**: high | medium | low
- **issue**: what the problem is (1-2 sentences)
- **suggestion**: what to do instead (1-2 sentences)

Severity: critical = blocks implementation; major = significantly affects quality/perf/maintainability; minor = nice-to-have.
Confidence: high = squarely in your domain; medium = relevant but could be wrong; low = outside core expertise.

Respond in the same language the artifact is written in.
```

`{PROFILE_PROMPT_AUGMENTATION}` подставляется из раздела `## Prompt augmentation` профиля; когда его
нет — пусто.

### Инвариантные правила

- **Никогда не показывать ревью одного агента другому** — независимость и есть весь смысл.
- **Все агенты получают один и тот же текст артефакта** — без пересказов и интерпретаций, и один и тот
  же текст промпта: различаются только роль и `PROFILE_PROMPT_AUGMENTATION`. Контекстный блок,
  собранный для одного ревьюера, обязан уйти всем остальным дословно.
- **Скелет промпта зафиксирован движком** — профили только дополняют, никогда не заменяют.
- **Со второго цикла панель сужается до владельцев рецидивирующей области.** Цикл 1 идёт полным
  составом профиля — он и обнаруживает, где трудное место. На цикле 2 запускаются только те
  ревьюеры, в чей домен попали находки цикла 1 с severity critical или major, плюс любой, чей
  `optional_if` сработал на новом тексте. Это **не** противоречит запрету сокращать панель из шага 2:
  тот запрет про первый прогон и про основание «эту область уже смотрели в прошлом ревью другого
  артефакта»; здесь основание другое — ревьюер, не давший ни одной значимой находки по этому тексту,
  на повторе даёт их ещё меньше, а стоит столько же. Состав цикла 2 и причину сужения записать в файл
  состояния строкой; при `--experts` сужение не применяется, состав задан явно.

## Шаг 4 — синтезировать вердикт

Прочитать все ревью. Правила агрегации движка, не переопределяемые:

| Сигнал | Действие |
|---|---|
| Severity critical при высокой confidence | блокер |
| Одна и та же проблема от двух и более агентов независимо | эскалировать до critical независимо от индивидуальной severity |
| Severity major при высокой domain_relevance | важное улучшение |
| Противоречащие мнения агентов | вынести как «неопределённость — требует решения»; молча не выбирать одно |
| Severity minor либо низкая confidence у одного агента | предложение |
| Флаг низкой domain_relevance | заметка, вес ниже |

Профиль добавляет `verdicts` (алфавит, например `[PASS, CONDITIONAL, FAIL]` либо
`[PASS, WARN, FAIL]`) и `severity_mapping` — для профилей на чеклистах вроде пунктов a–e тест-плана.

### Детектор churn (неисходимости) между циклами

Начиная со 2-го цикла, перед оформлением вердикта сравнить находки текущего цикла с находками
предыдущего цикла из `## Verdict History`: находка текущего цикла помечается `recurrence: true`, если
она попадает в ту же тему/область (тот же файл, механизм или содержательный дефект-класс — например
«граница недоверенного контента», «обработка конкурентности»), что и находка предыдущего цикла, уже
отмеченная там как исправленная, но проявляется как новый конкретный дефект в этой же области, а не
буквальный повтор старого. Это не текстовое совпадение — совпадение темы при разном конкретном
дефекте, что и означает: фикс предыдущего цикла не закрыл область, а сдвинул проблему внутри неё.
Признак в выводе: `recurrence: true` плюс одна строка, что именно повторяется и с какой находкой
предыдущего цикла она связана.

**Churn streak** — счётчик подряд идущих циклов с `recurrence: true`, хранится в файле состояния
(`Churn streak: N`, см. шаблон ниже). Цикл без `recurrence` сбрасывает счётчик в 0. Цикл 1 не может
иметь `recurrence` (сравнивать не с чем), streak на нём — 0.

**Инвариант:** два цикла подряд с `recurrence: true` (`Churn streak: 2`) → немедленная эскалация,
не дожидаясь исчерпания `--max-cycles`. Тип эскалации — `convergence_failure`, отдельный от обычного
`cycle_cap_exhausted`: одна и та же область не исходит к решению, а не просто кончился бюджет циклов.
Тип записывается в `## Escalations` файла состояния и в вывод пользователю. См. «После эскалации» ниже
— выбор пути возобновления зависит от типа.

### Формат вердикта

```
## Multi-Expert Review Verdict: {PASS | CONDITIONAL | WARN | FAIL}

### Blockers (must fix)
- {issue} — raised by {agent(s)}, severity: critical / Suggestion: {что делать}

### Important Improvements (strongly recommended)
- {issue} — raised by {agent(s)}, confidence: {уровень}

### Suggestions (nice to have)
- {issue}

### Uncertainties (requires your decision)
- {тема} — {Agent A} говорит X, {Agent B} говорит Y

### Consensus
{в чём сошлись все агенты}

## Review Mode: single-perspective       # только когда сработал путь единственного рецензента
```

**Случай одного агента:** разделы с перекрёстными ссылками пропустить, показать проблемы напрямую и
добавить маркер `## Review Mode: single-perspective`.

### Критерии вердикта

- **PASS** — блокеров нет, важных улучшений нет, только мелкие предложения.
- **CONDITIONAL** (только в алфавитах, где он есть) — блокеров нет, но важные улучшения существенно
  повлияли бы на качество.
- **WARN** (только в алфавитах, где он есть) — блокеры закрыты, но нарушены вторичные пункты
  (например (d) и (e) тест-плана); конвейер продолжается.
- **FAIL** — есть блокеры.

## Шаг 5 — действие после ревью

### Маршрутизация правок

По `profile.source_routing`:

| Источник | Действие по умолчанию |
|---|---|
| **Режим планирования** | вход в режим планирования со списком проблем |
| **Файл** | править файл напрямую (добавить `## Issues to Resolve` либо перестроить по месту) |
| **Разговор** | вынести пункт с наивысшей severity первым, ОДИН вопрос за раунд, идти по пунктам. Не вываливать список целиком. |

Профили могут переопределить действие либо пометить его `N/A` для источников, которые не
поддерживают.

### Работа с распиской

Если у профиля есть `profile.receipt`, разрешить `receipt.path_template` подстановкой `<slug>` и
записать каждое поле из `receipt.fields_to_update` вычисленным значением (`review_verdict: WARN`,
`review_warnings: [...]`, `review_blockers: [...]`). Для путей `swarm-report/...` — создать, если
отсутствует, соблюдая контракт расписки тест-плана из
`write-plan/references/test-plan-receipt.md`. `profile.receipt` отсутствует — запись расписки
пропустить.

### Обработка вердикта

- **PASS** — подтвердить, что артефакт готов; завершить.
- **CONDITIONAL** — показать улучшения в чате пунктами (не более 5; больше — сгруппировать по
  категориям). ОДИН вопрос, если нужно решение пользователя. После подтверждения править по
  `source_routing`.
- **WARN** — конвейер продолжается; движок записывает предупреждения в расписку; цикла правок нет.
- **FAIL** — править по `source_routing` без вопросов; автоматически перезапустить ревью с теми же
  агентами и тем же профилем; обновить файл состояния циклом N и новым вердиктом. Всё ещё FAIL на
  пределе `M` — эскалация пользователю по протоколу ниже, не автономная правка вне движка.

## После эскалации: возобновление

Эскалация — не тупик конвейера и не точка, после которой вызывающий чинит артефакт сам и объявляет
его готовым. **Правка по указанию пользователя открывает право на повторный прогон этим же движком,
а не заменяет его.** Гейт «готово» — фактический PASS или CONDITIONAL этого движка на актуальном
содержимом артефакта, никогда факт «пользователь одобрил конкретную правку».

Протокол для вызывающего (write-plan, write-spec, инлайновый вызов) после эскалации:

1. Показать пользователю находки предела (уже сделано на шаге эскалации), включая тип эскалации
   (`cycle_cap_exhausted` или `convergence_failure`) и, если применимо, тему рецидивирующей находки из
   детектора churn.

2. **Предложить меню путей возобновления**, не один-единственный «расширить бюджет». Рекомендованный
   вариант зависит от типа эскалации и характера находок — движок выбирает рекомендацию, решение всё
   равно за пользователем:

   | Путь | Когда рекомендуется по умолчанию |
   |---|---|
   | **Сузить объём: вырезать рецидивирующую область из артефакта** | **По умолчанию при `convergence_failure`.** Область выносится в отдельную фазу с owner и триггером расфриза, её цена называется в артефакте явным ограничением, остальное продолжает жить. Замерено на трёх прогонах одной фичи: сужение разрешало эскалацию оба раза, когда его применяли, и следующий цикл не возвращал в вырезанной области ни одной находки; прогон, где вместо сужения шли новые раунды, не сошёлся за девять циклов. Вопрос пользователю формулируется как «что вырезаем», а не «продолжаем ли». |
   | **Подключить `hard-case-arbiter`** как дополнительного или замещающего ревьюера на возобновлённых циклах | `convergence_failure`, когда вырезать нечего: рецидивирующая область неотделима от сути артефакта либо является его единственным содержанием. Арбитр ставит под сомнение постановку задачи; если и он не разводит, остаётся возврат на предыдущий этап. |
   | **Вернуться на предыдущий этап конвейера** (план → спека, спека → research) | Рецидивирующий дефект трассируется к одному структурному решению более раннего этапа, а не к деталям реализации текущего артефакта. |
   | **Продолжить тем же составом/моделью, расширить `--max-cycles`** | `cycle_cap_exhausted` **без** `recurrence` в истории — находки мелкие и механические. При `convergence_failure` не рекомендовать: это ровно тот путь, который на замере не сходился. |
   | **Поднять модель ревьюеров** (`--reviewer-model=opus` явно; если уже opus — поднять effort) на возобновлённых циклах | Находки тонкие/взаимодействующие и текущая модель их плохо ловит либо чинит с новыми побочными дефектами. |

   В интерактивном режиме или при присутствии пользователя — `AskUserQuestion` с этими вариантами,
   рекомендованный путь первым и с обоснованием. В headless-режиме — не требовать решения без
   предложения: записать рекомендацию (какой путь и почему) в блокирующий открытый вопрос и ждать.
   В обоих режимах решение остаётся за пользователем — движок сам путь не выбирает.

3. Внести правку в источник по выбранному пользователем пути. Для «расширить бюджет» и «поднять
   модель» — правка по разрешённому блокеру (та же маршрутизация `source_routing`, что и у обычного
   FAIL). Для «hard-case-arbiter» — сначала прогон этого агента, затем правка по его вердикту. Для
   «вернуться на предыдущий этап» — маршрутизация не в этот движок, а в скилл предыдущего этапа
   (`write-spec`, `research`); возврат в `multiexpert-review` происходит после того, как предыдущий
   этап переутверждён.
4. **Возобновить `multiexpert-review` тем же профилем**, с составом и моделью по выбранному пути
   (тот же состав по умолчанию; расширенный или замещённый `hard-case-arbiter`-ом состав, если выбран
   этот путь; `--reviewer-model=opus`/выше, если выбран путь поднятия модели), передав
   `--max-cycles=<M + исходный_предел>` (по умолчанию исходный предел — 2, то есть новый потолок 4).
   Счётчик цикла продолжается от места остановки (`Cycle: {M+1} of {M+3}`), а не сбрасывается на
   `Cycle: 1 of 3` — история прежних циклов в `## Verdict History` остаётся.
5. Результат этого прогона — обычный PASS/CONDITIONAL/WARN/FAIL, обрабатывается по тем же правилам
   шага 5. Артефакт можно двигать дальше по конвейеру (`approved`, следующая фаза) только на
   PASS/CONDITIONAL/WARN.
6. Эта конкретная расширенная попытка тоже дошла до предела и снова FAIL, либо снова дала два цикла
   подряд с `recurrence: true` — эскалировать заново, тем же протоколом (шаги 1–5), с новым выбором из
   меню. Рекурсия сама по себе не опасна: каждый шаг обусловлен свежим настоящим решением
   пользователя, а не тихим самопродлением движка.

**Единственный способ пойти дальше без фактического PASS/CONDITIONAL** — явный оверрайд пользователя
его собственными словами («не гоняй ревью ещё раз», «пропусти проверку», «двигаемся без ревью»).
Тогда вызывающий:

- **не** пишет `review_verdict: approved`/`pass` — пишет отдельное, отличимое значение
  (`review_verdict: overridden_by_user`) плюс дословную причину пользователя;
- фиксирует это в `## Escalations` файла состояния перед его удалением и в самом артефакте (например,
  `plan.md`'s `## Open Questions` записью с пометкой `[unreviewed]`), чтобы потребители ниже по
  потоку (`/acceptance`, `drive-to-merge`, будущий читатель) могли отличить отревьюенный артефакт от
  того, что прошёл мимо ревью по решению пользователя.

Одобрение пользователем содержания конкретной правки не эквивалентно оверрайду требования пройти
ревью — это два разных решения, и первое без второго не снимает гейт.

## Семантика ошибок

Все ошибки движка выдают ровно такой префикс в первой строке вывода:

```
[multiexpert-review ERROR] <CATEGORY>: <details>
```

Категории:

- `UNKNOWN_PROFILE_HINT` — подсказка вызывающего отсутствует в инвентаре.
- `FORBIDDEN_PROFILE_FIELD` — во frontmatter профиля есть запрещённое поле.
- `NO_REVIEWERS_AVAILABLE` — после обнаружения и фильтрации агентов не осталось либо профиль требует
  панель, а доступен один.
- `AMBIGUOUS_REVIEWER` — короткое имя разрешается в несколько файлов агентов после разрешения
  коллизии внутри семейства.
- `PROFILE_INVENTORY_MISMATCH` — список в README и фактическое наличие `profiles/*.md` расходятся.
- `ROUTING_NOT_SUPPORTED` — движок дошёл до шага 5 с источником, который профиль объявил `N/A` в
  `source_routing`.

Потребители (например `write-spec`) определяют этот префикс, чтобы отличать ошибки движка от обычного
вердикта FAIL.
